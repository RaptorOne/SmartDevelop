<UserControl x:Class="SmartDevelop.View.Projecting.ProjectTemplateItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="76" d:DesignWidth="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Image Width="50" Height="50" HorizontalAlignment="Center" Margin="7" Stretch="Fill" VerticalAlignment="Center" 
               Source="{Binding Image}"/>
        <TextBlock Grid.Column="1" HorizontalAlignment="Stretch" Margin="7" Text="{Binding Name}" VerticalAlignment="Center" />

        <TextBlock Grid.Column="2" HorizontalAlignment="Left" Margin="7" Text="{Binding Language.Name}" VerticalAlignment="Center" />
    </Grid>
</UserControl>
